`profile/images/banners/*` are generated artifacts.

Do not edit them manually.

Edit:

- `profile/images/source/cda-enterprise-ecosystem-banner.png`
- `profile/mantras.yml`

Then run:

```sh
npm run generate:banners
```

Validate reproducibility with:

```sh
npm run check:banners
```
